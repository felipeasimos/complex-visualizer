# Complex Number Visualizer

Allows for visualizing:
* Rotation - `z * e^(i*theta)`
* Translation - `z + w`
* Scaling - `z * a`
* Length - `|z|² = (a + b * i) * (a - b * i)`
* Conjugate - `a - b * i`
